package com.robotplatform.service.dify.api.appinit;

/**
 * Dify parameters API请求对象
 * 由于这是GET请求，所以没有请求体参数
 */
public class ParametersRequest {
    // 这是一个GET请求，不需要请求体参数
    // 如果将来需要添加查询参数，可以在这里扩展
}
